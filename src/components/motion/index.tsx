'use client';

// Direct import from framer-motion
import { 
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useScroll,
  useTransform,
  Variants,
  Transition,
  MotionProps
} from 'framer-motion';

// Export everything directly
export {
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useScroll,
  useTransform
};

// Export types with 'export type' syntax for TypeScript isolatedModules
export type { Variants, Transition, MotionProps };
